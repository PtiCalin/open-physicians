# CMQ Source Module

This module fetches and parses the College des medecins du Quebec (CMQ) registry. The dataset is publicly accessible and refreshed weekly. Use `fetch.py` to download the latest export and `parse.py` to convert rows into the canonical schema.
